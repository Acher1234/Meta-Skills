# Install Pillow

```bash
pip install pillow
# or
python3 -m pip install pillow
```

Via Meta-Skills (shared venv):

```bash
cd ~/.meta-skills/skills/design/gif-creator
~/.meta-skills/install.sh pip init .
```

Check the version:

```python
import PIL
print(PIL.__version__)  # e.g. 12.3.0
```
