# Origin

Vendored (copied, not a git submodule) from:

[grandcamel/JIRA-Assistant-Skills](https://github.com/grandcamel/JIRA-Assistant-Skills.git)

What was taken: upstream `skills/` markdown, consolidated into this single skill under `command.md/` and `exemple.md/`.

Local additions (not upstream): `SKILL.md`, `.env.example` (`JIRA_SITE_URL`, `JIRA_EMAIL`, `JIRA_API_TOKEN`), and `scripts/skill_env.py` (SkillCred + shell exports).

To refresh docs from upstream, re-copy markdown into `command.md/` / `exemple.md/` (not separate sub-skill installs).
