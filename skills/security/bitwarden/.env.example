# Self-hosted Bitwarden / Vaultwarden — copy to `.env` (gitignored).
# https://bitwarden.com/help/cli/

BW_SERVER=https://vault.example.com
BW_CLIENTID=
BW_CLIENTSECRET=
BW_PASSWORD=

# NODE_EXTRA_CA_CERTS=   # only if private CA / TLS inspection
